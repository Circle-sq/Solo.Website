export enum ActionType {
    Set = 'set',
    Start = 'start',
    Stop = 'stop',
    Reset = 'reset',
    Pause = 'pause',
}

export enum TimerStatus {
    Running = 'running',
    Paused = 'paused',
    Stopped = 'stopped',
}

export default TimerStatus;
