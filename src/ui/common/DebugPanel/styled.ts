import styled from '@emotion/styled';

export const S_DebugPanel = styled('div')`
    position: fixed;
    top: 0;
    left: 50%;
    width: 150px;
    z-index: 100000000;
    background: red;
    text-align: center;
    cursor: pointer;
`;
