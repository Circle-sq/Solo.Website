import styled from '@emotion/styled';

import { cssColor } from '@solo-ui/system';

export const S_CountryList = styled.ul`
    list-style: none;
    margin: 0;
    padding: 0;
    font-size: 14px;
    border-top: 1px solid ${cssColor('--list-lhn-border')};
`;
