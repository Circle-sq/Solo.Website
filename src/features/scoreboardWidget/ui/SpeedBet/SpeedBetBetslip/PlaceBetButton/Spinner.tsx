import { S_Spinner } from './styled';

const Spinner = () => {
    return (
        <S_Spinner>
            <span />
            <span />
            <span />
        </S_Spinner>
    );
};

export default Spinner;
