import styled from '@emotion/styled';

import { GreenPalette, fontWeight } from '@solo-ui/system';

export const S_PossibleWinnings = styled.div`
    font-size: 12px;
    line-height: 1.4;
    font-weight: ${fontWeight.regular};
    color: ${GreenPalette.green1};
    margin-top: 4px;
    text-align: center;
    min-height: 17px;
`;
