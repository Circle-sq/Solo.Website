import styled from '@emotion/styled';

import { cssColor } from '@sc-ui/system';

export const S_BetslipNotification = styled.div`
    padding: 5px 16px;
    background: ${cssColor('--box-default-bg')};
`;
