export const radius = Object.freeze({
    main: '4px',
    selection: '3px',
    secondary: '3px',
    wrapper: '6px',
    toggle: '2px',
} as const);

export const selections = Object.freeze({
    gutter: '8px',
} as const);
