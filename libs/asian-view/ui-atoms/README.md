# ui-asian-view

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test ui-asian-view` to execute the unit tests via [Vitest](https://vitest.dev/).


## Generate storybook for new component

```bash
$ npx nx g @nx/react:component-story --componentPath=lib/TennisScoreTable/TennisScoreTable.tsx --project=ui-asian-view --no-interactive --dry-run --verbose
```
